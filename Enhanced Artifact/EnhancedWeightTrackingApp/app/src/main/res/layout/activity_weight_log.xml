<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Display Goal Weight -->
    <TextView
        android:id="@+id/goal_weight_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Goal Weight: 110.0 lbs"
        android:textSize="18sp"
        android:paddingBottom="8dp"
        android:textStyle="bold"/>

    <!-- Weight Prediction TextView (new addition) -->
    <TextView
        android:id="@+id/weight_prediction_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Weight prediction will appear here"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:padding="8dp"
        android:gravity="center"
        android:paddingBottom="8dp"
        android:textStyle="italic" />

    <!-- Display Weight History Label -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Weight History:"
        android:textSize="18sp"
        android:paddingBottom="8dp"
        android:textStyle="bold"/>

    <!-- ListView for Weight Log -->
    <ListView
        android:id="@+id/weight_log_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"/>

    <!-- Buttons for Adding Weight, Setting Goal, and SMS Notifications -->
    <Button
        android:id="@+id/add_weight_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Weight" />

    <Button
        android:id="@+id/set_goal_weight_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Set Goal Weight" />

    <Button
        android:id="@+id/sms_notification_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="SMS Notifications" />

</LinearLayout>
